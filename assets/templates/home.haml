- extends "layout/base.html"
- load static

- block body
  .body-container
    %h1.section-header Recently Added Replays
    .thumb-container
      - for r in replays
        - include "partials/replay_thumb.html" with replay=r